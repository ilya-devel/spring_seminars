spring:
  application:
    name: sem12_homework
  datasource:
    url: jdbc:postgresql://localhost:5432/sem12_homework
    username: ilya-devel
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
